# Tutorials for TDA4j

TODO: We want to port Henry Adams' excellent JavaPlex tutorials.

Placeholder page; nothing ported yet. When per-language (Java/Scala) tabbed examples are needed here, Laika's
equivalent of Paradox's language-group tabs is the `@:select`/`@:choice` directive pair, backed by a
`laika.selections` block in `directory.conf` — not yet wired up since there's no real content to switch between.